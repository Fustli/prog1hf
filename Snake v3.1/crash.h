#ifndef CRASH_H_INCLUDED
#define CRASH_H_INCLUDED
#include "snake.h"

bool crash(listData* first);
bool crash2(listData* first);
bool suicide(listData* first);
bool suicide2(listData* first);

#endif // CRASH_H_INCLUDED
